"use strict";

export class Tasks{
  constructor(text){
    let txt = text;
    this.task = {
      text: txt,
      done: false 
    }
    // this.arr = arr;
    // this.arr.push(this.task);
  }
  checking(){
    this.task.done != this.task.done;
  }
}